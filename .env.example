# Keycloak Configuration
VITE_KEYCLOAK_URL=http://localhost:8081
VITE_KEYCLOAK_REALM=vue-app
VITE_KEYCLOAK_CLIENT_ID=vue-template

# Application Configuration
VITE_APP_TITLE=Vue Template
VITE_API_BASE_URL=/api
VITE_APP_DEBUG=false

# Note: When running with Docker Compose, these values are automatically
# used as build arguments. No need to pass them separately.
